// Stub for jimp — nut-tree-fork/nut-js depends on jimp for image features,
// but we only use keyboard simulation. This stub avoids bundling the real jimp
// (which has deep CJS compatibility issues in ESM builds).
export default {};
